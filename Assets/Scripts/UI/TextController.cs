using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class TextController
{
    /*
    public static IEnumerator FadeOut(TextMeshProUGUI text, float t)
    {
        float startOpacity = audioSource.volume;

        while (audioSource.volume > 0)
        {
            audioSource.volume -= startVolume * Time.deltaTime / t;

            yield return null;
        }

        audioSource.Pause();
        audioSource.volume = startVolume;
    }
    public static IEnumerator FadeIn(AudioSource audioSource, float t)
    {
        float endVolume = audioSource.volume;
        audioSource.volume = 0;
        audioSource.UnPause();

        while (audioSource.volume < endVolume)
        {
            audioSource.volume += endVolume * Time.deltaTime / t;

            yield return null;
        }

        audioSource.volume = endVolume;

    }
    */
}
